# 导出首页给我重建

由于当前工作环境无法直接解析 `anfilm.club`，请在你能打开网站的浏览器中保存首页：

## 推荐：SingleFile

1. 在 Chrome 安装 **SingleFile** 扩展。
2. 打开 `https://www.anfilm.club/` 首页。
3. 点击 SingleFile 图标，等待保存完成。
4. 将生成的 `.html` 文件上传给我。

## 不安装扩展

在 Chrome 中选择“文件 → 将网页另存为”，保存类型选择“网页，完整”，然后把生成的 `.html` 文件和同名资源文件夹一起压缩成 ZIP 上传。

如果首页包含视频、表单或 Google Drive 文件，还请一并提供这些资源的原始链接；它们不能总是随网页自动保存。
